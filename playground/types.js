/* eslint-disable*/
/*
const name = 'wes';
const middle = "topher";
const last = `bos`;

const sentence = "she's so cool"

const sentence2 = `she's so cool`;

const hello = 'Hello my name is ' + name + '. Nice to meet you';

const hello2 = `Hello my name is ${name}. Nice to meet you`;


const html = `
 <div>  
 <h2>${name}</h2>
 <p>${hello}</p>
 </div>
`
document.body.innerHTML = html;


const age = 100;
const name = 'wes';

//const a = 30;
//const b = 20;

const smarties = 20;
const kids = 3;
const each_kids_gets = Math.floor(smarties/kids);
const dad_gets = smarties % kids;

console.log(`There are ${smarties} smarties and ${kids} kids`);
console.log(`Each kids gets ${each_kids_gets}`);
console.log(`Dad gets ${dad_gets}`);

*/
const person ={
    first: 'wes',
    last: 'boss',
    age: 100
};

let dog;
console.log(dog);